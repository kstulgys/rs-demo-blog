import React from 'react';
import { Box, Flex, Text } from '@chakra-ui/core';

const Container = props => (
  <Flex {...props} maxWidth="5xl" width="full" mx="auto" px="4" />
);

export default Container;
